from setuptools import setup
from os.path import join
import os

project_name = "diary"

setup(
        name=project_name,
        version="0.1.0",
        packages=[project_name],
        zip_safe=True
)
